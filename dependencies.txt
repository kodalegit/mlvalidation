django
scikit-learn==1.2.2
sudo apt update
sudo apt install nodejs
sudo apt install npm
in package.json directory: npm install
pandas
xgboost==1.7.6
(For production to get latest version of node: 
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash
[close terminal and open again]
[confirm installation with "command -v nvm]
nvm install --lts
)

(cut away .env outside root dir)
use npx webpack to bundle react js then collectstatic before deployment
gcloud app deploy --no-cache
